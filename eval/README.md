# Evaluation

Run all supplied cases from the repository root:

```bash
cd backend
pip install -r requirements.txt
cd ..
python eval/run_eval.py
```

The runner writes `eval/eval_report.json` with the complete response and trace for every case.
